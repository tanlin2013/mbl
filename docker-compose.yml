services:
  mbl:
    image: tanlin2013/mbl
    pull_policy: always
    shm_size: 10.07gb
    network_mode: "host"
    volumes:
      - ~/projects/mbl/scripts:/home/mbl/scripts
      - ~/.aws:/root/.aws
    command: python scripts/sampler.py
